
import { computerInventory } from 'computer-store/helpers/computer-inventory';
import { module, test } from 'qunit';

module('Unit | Helper | computer inventory');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = computerInventory([42]);
  assert.ok(result);
});

